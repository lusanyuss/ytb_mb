<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===============被按=================== -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">

            <!-- 填充的颜色 -->
            <gradient android:angle="90" android:endColor="#e5e5e5" android:startColor="#e5e5e5" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:bottomLeftRadius="5dp" android:bottomRightRadius="5dp" />
            <!-- 边框 -->
            <stroke android:width="1dp" android:color="#e5e5e5" />
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="0dp" />
        </shape>
    </item>
    <!-- ===============正常=================== -->
    <item android:state_enabled="true">
        <shape android:shape="rectangle">

            <!-- 填充的颜色 -->
            <gradient android:angle="90" android:endColor="#f7f7f7" android:startColor="#f7f7f7" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:bottomLeftRadius="5dp" android:bottomRightRadius="5dp" />
            <!-- 边框 -->
            <stroke android:width="1dp" android:color="#f7f7f7" />
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="0dp" />
        </shape>
    </item>

</selector>